const AccountNumber = "010101010132"; //If you want to change the account number, you can do so by modifying this variable.
let AccountEmail = "admin123@gmail.com"; //If you want to change the account email, you can do so by modifying this variable.
let AccountPassword = "admin123"; //If you want to change the account password, you can do so by modifying this variable.
let AccountBalance = 1000; //If you want to change the account balance, you can do so by modifying this variable.
let AccountHolderName = "Vatsal Shah"; //If you want to change the account holder name, you can do so by modifying this variable.
let AccountHolderAddress = "Ahmedabad, Gujarat, India"; //If you want to change the account holder address, you can do so by modifying this variable.
let AccountHolderPhoneNumber = 9876543210; //If you want to change the account holder phone number, you can do so by modifying this variable.
let AccountHolderDOB = "10/08/2004";
let AccountNominee; // This variable is declared but not initialized, so it will be undefined.
console.table([AccountNumber, AccountEmail, AccountPassword, AccountBalance, AccountHolderName, AccountHolderAddress, AccountHolderPhoneNumber, AccountHolderDOB, AccountNominee]);
